services:
  - type: worker
    name: ascension-discord-bot
    runtime: docker
    plan: free
    region: frankfurt
    envVars:
      - key: DISCORD_TOKEN
        sync: false
      - key: DATABASE_URL
        value: "file:/data/bot.db"